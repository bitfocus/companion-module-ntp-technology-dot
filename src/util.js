/* const { regexpAddr } = require('./consts.js') */
module.exports = {
	/* 	regexAddress(addr) {
		let addrress
		let safeAddress
		while ((addrress = regexpAddr.exec(addr)) !== null) {
			safeAddress = addrress[0]
		}
		safeAddress = safeAddress != undefined ? safeAddress : false
		return safeAddress
	}, */
}
